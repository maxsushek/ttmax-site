// src/data/catalog/priority.ts
// ⚙️ ФАЙЛ ДЛЯ ВЛАСНИКА. Тут — і тільки тут — задається, які товари просувати першими.
// Правити можна без розробника: це звичайний список слагів.
//
// ────────────────────────────────────────────────────────────────────────────
// 1) MARGIN_PRIORITY — топ по маржі
// ────────────────────────────────────────────────────────────────────────────
// Слаги товарів, які вигідніші за інших. Вони отримують невеликий бонус у блоці
// «Схожі товари» — тобто за інших рівних показуються раніше.
//
// ⚠️ СВІДОМО НЕВЕЛИКИЙ бонус (PRIORITY_BONUS нижче), а не абсолютний пріоритет.
// Якщо тягнути дороге туди, де воно НЕ підходить (напр. пропонувати OFF+ флагман
// до основи для новачка), падає і довіра, і конверсія — людина бачить, що їй
// впарюють, а не радять. Тому релевантність завжди важливіша за маржу:
// бонус вирішує лише між кандидатами, які й так підходять.
//
// Слаг = останній сегмент URL картки. Приклад:
//   /ua/butterfly/nakladki/tenergy-05  →  "tenergy-05"
//   /ua/butterfly/osnovaniya/viscaria  →  "viscaria"
//
// Порожній список = маржа не враховується, працює чиста релевантність.
export const MARGIN_PRIORITY: readonly string[] = [
  // Приклад (закоментовано — розкоментуйте й замініть на свої):
  // "tenergy-05",
  // "dignics-09c",
  // "viscaria",
];

/**
 * Наскільки сильно маржа зсуває товар угору.
 * Для орієнтиру: та сама серія дає +40, той самий тип поверхні +24.
 * 12 — це «за інших рівних показати першим», а не «показувати завжди».
 */
export const PRIORITY_BONUS = 12;

/**
 * Мінімум вхідних посилань на кожну картку money-категорій.
 * Навіщо: аудит показав 14 money-товарів із НУЛЕМ вхідних крос-селів — на них не вело
 * жодне посилання з інших карток, тобто Google діставався до них тільки з лістингу.
 * П'ять із семи таких накладок були Tenergy — саме та серія, куди треба заганяти вагу.
 */
export const MIN_IN_DEGREE = 3;
