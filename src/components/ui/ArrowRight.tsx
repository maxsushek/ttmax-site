/**
 * Стрілка «далі» як SVG замість символу →.
 *
 * ⚠️ НЕ повертати назад текстовий → у кнопки. Заміряно на живому проді через PageSpeed:
 * один цей символ робив кандидатами два підмножинні файли Roboto — «math» (37 КБ) і
 * «symbols» (18 КБ), яких немає серед чотирьох префетчених. Вони висіли в критичному
 * ланцюжку й забивали канал саме тоді, коли сторінка малювалась: у розбивці LCP TTFB
 * був 10 мс, а «затримка відмальовки елемента» — 2500 мс.
 *
 * Спроба лишити символ і підмінити його системним шрифтом через @font-face з local()
 * спрацювала лише частково: якщо в локальному шрифті потрібного знака немає, браузер
 * усе одно йде качати підмножину. SVG знімає питання назовсім.
 *
 * currentColor + em-розміри — стрілка успадковує колір і масштаб тексту, як гліф.
 */
export function ArrowRight({ className = "" }: { className?: string }) {
  return (
    <svg
      aria-hidden
      viewBox="0 0 16 12"
      className={`ml-1.5 inline-block h-[0.72em] w-[0.96em] align-[-0.05em] ${className}`}
      fill="none"
      stroke="currentColor"
      strokeWidth="1.7"
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      <path d="M1 6h13M9.5 1.5L14 6l-4.5 4.5" />
    </svg>
  );
}
