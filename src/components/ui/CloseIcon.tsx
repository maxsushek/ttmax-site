/**
 * Хрестик «закрити» як SVG замість символу ✕ (U+2715).
 *
 * ⚠️ НЕ повертати текстовий ✕. Він стоїть у кнопках закриття мобільного меню, кошика,
 * чекауту й швидкого замовлення — тобто присутній на КОЖНІЙ сторінці сайту. Через це
 * він сам-один робив кандидатом підмножину «symbols» у Roboto (18 КБ), якої немає серед
 * префетчених шрифтів: файл висів у критичному ланцюжку на всіх сторінках одразу.
 *
 * Перевірено після заміни ₴ і стрілок: на /nakladki і на картці товару з усіх «дорогих»
 * символів лишався рівно цей хрестик.
 *
 * currentColor + em-розмір — успадковує колір і кегль кнопки, як гліф.
 */
export function CloseIcon({ className = "" }: { className?: string }) {
  return (
    <svg
      aria-hidden
      viewBox="0 0 14 14"
      className={`inline-block h-[0.85em] w-[0.85em] ${className}`}
      fill="none"
      stroke="currentColor"
      strokeWidth="1.9"
      strokeLinecap="round"
    >
      <path d="M2 2l10 10M12 2L2 12" />
    </svg>
  );
}
