# Передіндексний аудит TT-Max
**Дата:** 25.07.2026 · **Прод:** `https://ttmax-site-z2za.vercel.app` · **Стан:** `launched=false`, `robots.txt: Disallow: /`, всі 696 URL віддають `noindex, follow`

---

## Вердикт: чи можна знімати noindex

**Ні. Не раніше, ніж закриті 3 пункти P0.** Головний — сайт живе на `ttmax-site-z2za.vercel.app`, і цей хост зашитий у **всі** сигнали (canonical, 3×hreflang, og:url, Organization/WebSite/Product JSON-LD, 696 `<loc>` у sitemap, Host/Sitemap у robots.txt — від 16 до 26 входжень на сторінку). Зняти noindex зараз = проіндексувати магазин під доменом із Public Suffix List і потім робити повну 301-міграцію з просадкою. Другий і третій блокери — контакти: у футері **кожної** сторінки висить клікабельний `tel:+380000000000` з підписом «+380 (XX) XXX-XX-XX» (і той самий рядок у `Organization.contactPoint.telephone`), а `/contacts` текстом зізнається «Точну адресу магазину та телефон додамо найближчим часом». Все інше в звіті — це втрати й гігієна, які можна робити паралельно, але вони не гейтять запуск.

**Мінімальний шлях до індексу:** домен → конфіг хоста → контакти/телефон → GSC+GA4 → preview-деплой з `launched=true` для перевірки → флип. Реалістично 1–2 робочі дні, якщо домен реєструється сьогодні.

---

## Кореневі причини

### R1. Рендерер метаданих отримує тільки `{name, category, price}`
Найдорожча причина: на картці лежить 1200+ знаків унікального експертного тексту, «Короткий вердикт», три числові оцінки, іменований КМС — і **жодне** з цих полів не доходить до `generateMetadata` і до `Product` JSON-LD.

Доказ: `curl /ua/butterfly/osnovaniya/viscaria` → `Product` keys = `name, description, url, brand, image, sku, offers`; `Product.description` **дослівно дорівнює** meta description = `«Купити Butterfly Viscaria в Україні. Характеристики, ціна, відгуки. Гарантія, доставка.»` При цьому на тій самій сторінці рендериться «Еталонна outer-ALC основа OFF+: швидкість, чіткий прямий відскік…».

**Симптоми:** шаблонний description на 620 із 622 карток · «відгуки» в title і description при 0 `aggregateRating` на сайті · відсутні `gtin/mpn/shippingDetails/hasMerchantReturnPolicy/seller` · відсутність кириличних форм · відсутність автора/E-E-A-T на money-картках · відсутність `dateModified` · 1094 порожніх `alt` у галереях. **Одна правка закриває ~12 знахідок.**

### R2. Хаби серій згенеровані без єдиного власника істини
Поріг «≥2 товари» застосований **лише** в `sitemap.ts`. Роутинг, генератор title і навігація вирішують незалежно.

Наслідок-парадокс, перевірений curl:
- у sitemap 9 серійних хабів накладок, з них **6 порожніх** (`feint` 268 знаків, `tackiness` 185, `sriver` 157, `impartial` 145, `roundell` 136, `ilius` 123 — H2=0) і **всі 6 мають 0 внутрішніх лінків**;
- поза sitemap — `zyre` (3 222 знаки, 9×H2, лінкується сайтвайдно з хедера) і `rozena` (2 391 знак, 9×H2);
- ще 3 однотоварні хаби (`bryce`, `aibiss`, `flextra`) віддають 200 і не мають ані лінків, ані sitemap.

Критерій відбору прив'язаний до **кількості товарів**, а має бути прив'язаний до **наявності унікального тексту**.

### R3. Три словники локалі: URL `ua`, BCP47 `uk`, БД `uk`
`/uk/nakladki` → 308 → `/ua/nakladki`, і водночас на `/ua/nakladki` в тілі емітяться `href="/uk/nakladki/dignics"`, `/uk/nakladki?surfaceType=gladka` тощо — **88 таких лінків на 10 сторінках**, майже всі на money-гілці. У БД `leads_locale_check` і `orders_locale_check` = `CHECK (locale IN ('uk','ru'))`, а Zod API приймає `'ua'|'ru'`; працює тільки завдяки мапінгу в одному шарі. У `content_blocks` розсинхрон уже матеріалізувався: 12 рядків `locale='ua'` + 12 `locale='uk'` на ті самі слаги (дублі створені бекфілом 19.06.2026), і CHECK на цій таблиці **немає взагалі**.

### R4. Прапорці запуску гейтять усе — жодна підсистема нижче ніколи не рендерилась із `launched=true`
16 перевірених URL (головна, всі 10 категорій, `/butterfly`, `/blog`, `/osnovaniya/alc`, `/nakladki/tenergy`, `/ru/rakety`) віддають ідентичний `noindex, follow`. Індексаційна частина аудиту перевіряється **тільки** непрямо — через sitemap (він коректно виключає 95 комбо-ракеток і 20 `/butterfly/{category}`, тобто дзеркалить `index`-флаг). Один коміт змінює стан 696 URL, який ніколи не бачили в рендері.

**Симптоми:** vercel-хост у всіх сигналах · плейсхолдер телефону · неіснуючий домен у e-mail · нуль аналітики · privacy обіцяє GA/GTM/Pixel, яких немає.

### R5. Два конвеєри контенту, і згенерований володіє 53% індексу
Money писали руками, хвіст згенерували. Розкладка sitemap: money 254 URL (36%), хвіст 368 URL (53%). У 183 із 184 gear-карток **нуль прози** (медіана prose-слів = 0; набір H2 дослівно `['Характеристики','Схожі товари','Кошик']`). 230 Product-блоків без `image` (chehly 98/98, setki 6/6 — 100% категорій без фото).

### R6. Продуктивність не була в скоупі жодного напряму
`/ua/osnovaniya` = 735 113 B сирого HTML. Причина не «84 товари», а **подвійний рендер сітки**: Suspense-fallback на `next/image` (84 `srcSet`, 178 452 B) + розв'язаний `CatalogFilters` у `<div hidden id="S:0">` (ще 84 плоских `img`). Ділянка `33610→358672` = 325 062 B = 44% сторінки. Плюс RSC-payload 248 152 B (34%). Те саме на `/ua/nakladki` (425 614 B). Усе це з `cache-control: private, no-cache, no-store` і `x-vercel-cache: MISS`, при обчисленні в `iad1` (US East) для української аудиторії.

---

## P0 — блокери індексації

### 1. Усі сигнали прибиті до `*.vercel.app`, а `ttmax.com.ua` не зареєстровано

**Що:** canonical, 3×hreflang, og:url, `Organization.url`, `WebSite.url`, `SearchAction.target`, `BreadcrumbList.item`, `Product.url`, `Product.offers.url`, 696 `<loc>` у sitemap, `Host:`/`Sitemap:` у robots.txt — усе на прев'ю-хості.

**Доказ:**
```
curl /robots.txt → Host: https://ttmax-site-z2za.vercel.app
curl /ua/nakladki | grep -c 'ttmax-site-z2za' → 20
curl /ua/butterfly/nakladki/dignics-05 | grep -c 'ttmax-site-z2za' → 26
curl /sitemap.xml | grep -c '<loc>' → 696 (усі на vercel.app)
curl --max-time 8 https://ttmax.com.ua/ → 000 (домен не резолвиться)
dig NS ttmax.com.ua → порожньо; whois → No entries found
```
Абсолютних внутрішніх `<a href>` у тілі — **нуль** (перевірено: 191 тег `<a>` на `/ua/nakladki`, жодного з абсолютним хостом). Тобто все походить з однієї конфігурації.

**Наслідок:** індексація під PSL-доменом → нуль накопиченої доменної авторитетності, повна 301-міграція 696 URL пізніше. Плюс після зняття Disallow обидва хости віддаватимуть однаковий контент.

**Фікс (порядок строго):** 1) зареєструвати домен, підключити до Vercel, зробити **Primary** (тоді vercel.app-аліас сам почне віддавати 308); 2) оновити хост у конфігу (`src/config/site.ts` / `NEXT_PUBLIC_SITE_URL`) — спершу відкрити файл і встановити, це env чи константа; 3) redeploy **чистим білдом** (sitemap генерується на білді); 4) перевірка: `curl -s https://ttmax.com.ua/sitemap.xml | grep -c vercel.app` → має бути `0` (зараз 2794 входження).

**Страховка від рецидиву:** build-time guard — валити білд, якщо `launched === true` і базовий URL матчить `/vercel\.app$/`. 5 рядків, робить помилку фізично неможливою.

**Money:** так — всі 254 money-URL.

---

### 2. Плейсхолдер телефону на кожній сторінці, включно з клікабельним `tel:`

**Що:** у футері всіх ~700 URL:
```html
<a href="tel:+380000000000" data-cta="phone" data-location="footer">+380 (XX) XXX-XX-XX</a>
```
і той самий рядок у структурованих даних:
```json
"contactPoint":[{"@type":"ContactPoint","telephone":"+380 (XX) XXX-XX-XX","email":"hello@ttmax.com.ua"}]
```

**Доказ:** `curl /ua | grep -c 'XXX-XX-XX'` → **5** входжень; підтверджено на `/ua`, `/ru`, `/ua/nakladki`, `/ua/osnovaniya`, `/ua/butterfly/nakladki/tenergy-05`, `/ua/contacts`, `/ua/about`, `/ua/delivery`. У RSC-payload видно джерело: `contact:{phone:"+380000000000", phoneDisplay:"+380 (XX) XXX-XX-XX"}` — два поля, і JSON-LD бере **display**, а не E.164.

Поруч: три соцкнопки з `href="#"` (`aria-label="telegram|youtube|facebook"`) на кожній сторінці, при `"sameAs":[]` в Organization і `social:{telegram:"",youtube:"",facebook:""}` у конфігу.

**Наслідок:** покупець на картці Tenergy 05 за 3 115 ₴ тисне «подзвонити» і набирає неіснуючий номер. Плюс placeholder-рядок у структурованих даних організації на всьому сайті.

**Фікс:** реальний номер у **обидва** поля (`phone` в E.164, `phoneDisplay` — людський формат) + перемкнути білдер JSON-LD на `contact.phone`, а не на `phoneDisplay`. Якщо номера ще немає — **прибрати блок телефону цілком**; мертвий `tel:` гірший за відсутність. Те саме з трьома `href="#"`. Приймальний тест: `curl` будь-якої money-картки не має містити ані `XXX-XX-XX`, ані `+380000000000`.

**Money:** так — усі money-картки й обидві money-категорії.

---

### 3. `/contacts` декларує відсутність адреси й телефону; e-mail на незареєстрованому домені

**Що:** увесь текст `<main>` `/ua/contacts` = 485 знаків:
> «E-mail: hello@ttmax.com.ua … Місто: Харків. Доставка — по всій Україні. **Точну адресу магазину та телефон додамо найближчим часом**; наразі замовлення зручно оформити онлайн або написати на e-mail. Пн–Сб: 10:00–19:00.»

Плюс: «Соцмережі (Telegram, YouTube, Facebook) — посилання у шапці та футері сайту» — при тому, що в шапці соцблоку **немає взагалі**, а у футері всі три = `href="#"`.

**Доказ:** `curl /ua/contacts` (67 782 B HTML, 485 знаків тексту), `curl /ru/contacts` (67 862 B). `dig MX ttmax.com.ua` → порожньо: скринька `hello@ttmax.com.ua` не існує і не існуватиме до делегування домену.

**Наслідок:** на день індексації в магазину немає жодного працюючого каналу зв'язку, крім форми (`/api/orders` і `/api/leads` живі — перевірено, віддають 400 на порожній body). Немає реквізитів суб'єкта господарювання, чого вимагає ЗУ «Про захист прав споживачів» ст. 15 для дистанційної торгівлі і без чого не проходить верифікація Merchant Center.

**Фікс:** реальний телефон + однозначне позиціонування («Онлайн-магазин, доставка з Харкова» — це цілком легітимно і не потребує вигадувати адресу) + блок реквізитів (ФОП/ТОВ, ЄДРПОУ/РНОКПП, відповідальний за обробку ПД) в **одному** джерелі, підтягнутому на `/privacy`, `/terms`, `/contacts` обома локалями. **Не** додавати `LocalBusiness` schema, якщо фізичної точки для відвідувачів немає — це порушення гайдлайнів і ризик санкцій; лишити `Organization`/`OnlineStore`.

**Money:** так — trust-сторінка перед покупкою основи за 8 835 ₴.

---

## P1 — суттєві втрати

### 1.1. Один шаблон meta description на 254 money-картки (і на 620 із 622 карток загалом)

**Доказ:** прогін усіх 127 UA money-URL із sitemap → після нормалізації назви лишається **рівно 1** скелет: `«Купити Butterfly {N} в Україні. Характеристики, ціна, відгуки. Гарантія, доставка.»` (85–117 знаків). RU дзеркально. Той самий рядок дублюється в `og:description`, `twitter:description` і `Product.description`.

При цьому **127/127 карток уже мають унікальний «Короткий вердикт»** (UA 113–295 знаків, RU 110–300), напр. Viscaria: «Еталонна outer-ALC основа OFF+: швидкість, чіткий прямий відскік і широка зона комфорту».

**Фікс (три різні поля, не один рядок):**
- `meta description` = перше речення вердикту (обрізка **по межі речення**, cap ~155) + `Butterfly {name} — ціна, наявність, доставка по Україні.`
- `og:description` = **повний** вердикт без обрізки
- `Product.description` = вердикт + абзац характеристик (232–464 знаки), без ліміту

**Обов'язково умовна гілка по ціні:** 5 карток не мають `price`/`availability` — `roundell`, `roundell-soft`, `bugller`, `feint-ox`, `fan-zhendong-cnf`. Хардкод «Ціна {price} ₴, є в наявності» дасть на них «Ціна  ₴» і брехню про наявність.

**Час:** 2–3 год. **Money:** так, 100% money-гілки.

---

### 1.2. 88 внутрішніх лінків `/uk/` у тілі money-категорії й серійних хабів

**Доказ (повний краул 696 URL + 2 хаби поза sitemap):**

| URL | лінків `/uk/` |
|---|---|
| `/ua/nakladki/tenergy` | 18 |
| `/ua/nakladki` | 14 |
| `/ua/aksessuary` | 11 |
| `/ua/nakladki/dignics` | 10 |
| `/ua/odyag` | 8 |
| `/ua/nakladki/rozena` | 6 |
| `/ua/nakladki/glayzer` | 6 |
| `/ua/nakladki/zyre` | 5 |
| `/ua/myachi` | 5 |
| `/ua/chehly` | 5 |
| **разом** | **88 на 10 сторінках, 46 унікальних URL** |

RU чиста (0). Sitemap чистий (0 із 696). Усі `/uk/` віддають 308 із збереженням query (`/uk/nakladki?surfaceType=gladka` → `/ua/nakladki?surfaceType=gladka`, final 200).

Джерело — авторський rich-text у `content_blocks`: блок «Типи накладок — як обрати», список «Популярні серії Butterfly», порівняльна таблиця з `data-label`.

**Уточнення до наслідку:** 308 передає PageRank повністю, і всі цілі паралельно залінковані з глобального меню — тобто жодна картка не осиротіла. Реальна шкода: 88 redirect-хопів на money-гілці перед першим краулом + втрата **контекстних** анкорів («Dignics», «Tenergy», «основу для ракетки») на користь навігаційних.

**Фікс:**
```sql
UPDATE content_blocks SET body = replace(body,'href="/uk/','href="/ua/')
WHERE locale IN ('ua','uk') AND body LIKE '%href="/uk/%';
```
**Не** робити «санітайзер, що нормалізує префікс до поточної локалі» — він мовчки переб'є навмисні крос-локальні лінки. Замість цього — CI-guard: падати, якщо у будь-якому `content_blocks`/HTML є `href="/(?!ua|ru)[a-z]{2}/`.

**Час:** 30 хв + 1 год на guard. **Money:** так.

---

### 1.3. Cross-sell підбирається за алфавітом слага, 14 money-SKU з 0 вхідних

**Доказ (curl, блок «Схожі товари»):**
- `apolonia-zlc` (ZLC-флагман, OFF+ атака, 12 995 ₴) → `balsa-carbo-x5`, `balsa-x7`, `dimitrij-ovtcharov-innerforce-alc` (OFF− універсал), `diode-pro` (OFF− універсал)
- `viscaria` (OFF+ атака, 8 835 ₴) → `viscaria-super-alc`, `xstar-v` (OFF− універсал, 2 595 ₴), `zhang-jike-alc`, `zhang-jike-super-zlc`
- `mizutani-super-zlc` → `mizutani-zlc`, `offensive`, `omar-assar`, `omar-assar-zlc` — **рівно 4 наступні слаги за алфавітом**

Механізм: спочатку «брати за префіксом серії», потім добирати алфавітними сусідами. Фільтра за `bladeClass`/`surfaceGroup`/ціною немає.

**Граф (розпарсив усі 311 товарних сторінок):** 0 вхідних cross-sell мають **14** money-SKU, а не 7: основи `timo-boll-control`, `timo-boll-forte`, `timo-boll-forte-iii`, `timo-boll-fortissimo`, `freitas-alc`, `franziska-innerforce-zlc`, `kenta-matsudaira-alc`; накладки `tenergy-25`, `tenergy-05-hard`, `tenergy-25-fx`, `tenergy-64-fx`, `tenergy-80-fx`, `zyre-03`, `flextra`. П'ять із семи — Tenergy, тобто саме та серія, куди треба заганяти вагу. Розподіл in-degree по основах: `{0:7, 1:5, 2:9, 3:13, 4:21, 5:13, 6:8, 7:3, 8:1, 12:4}`.

**Фікс — дві частини, друга обов'язкова:**
1. **Скоринг замість фільтрів:** бали за ту саму серію (найвище), той самий `surfaceGroup`/`surfaceType`, суміжний `bladeClass` (OFF+↔OFF, але **не** OFF+↔OFF−), близькість ціни. Топ-4 за сумою. Жорсткий фільтр стягне граф у кластери: `timo-boll-{zlc,zlf,spirit-off,offensive}` уже мають по 12 вхідних і лінкуватимуться один на одного, а `timo-boll-control/forte/forte-iii/fortissimo` (ALL/ALL+) залишаться на нулі назавжди.
2. **Мінімальний in-degree ≥ 3:** після побудови графа примусово догнати кожну картку, витісняючи найслабші позиції у перенасичених вузлів. Без цього 14 нульових карток лишаться нульовими.
3. Заодно полагодити 4 картки, що віддають по 2 лінки замість 4.

Ручний список власника «з чим реально пару беруть» для топ-10 основ і топ-10 накладок закриває п. 1 без евристики — але **не замінює** п. 2.

**Час:** 4–6 год. **Money:** так, весь товар-до-товару граф обох money-категорій.

---

### 1.4. Head-форма «накладки на ракетку» (80/міс) відсутня на дефолтній локалі

**Доказ:** `curl /ua/nakladki` → title «Накладки на **тенісну** ракетку — купити | Butterfly | TTMAX» (форма на 20/міс), H1 «Накладки для настільного тенісу Butterfly» (20/міс). Точна фраза «накладки на ракетку» у `<main>` (5 126 знаків) — **0 входжень**; навіть підрядок «на ракетку» — 0. `hreflang x-default` вказує на `/ua/nakladki`.

Ahrefs ua: `накладки на ракетку` **80**, `накладки на тенісну ракетку` 20, `накладки для настільного тенісу` 20, `накладки на ракетку для настольного тенниса` 30.

Бонус, який пропустили: H1 `/ru/nakladki` = «Накладки для ракеток настольного тенниса» — Ahrefs ua дає цій фразі **0**, тоді як «накладки для настольного тенниса» = 30.

**Фікс:**
- UA metaTitle → `Накладки на ракетку для настільного тенісу — купити | Butterfly | TTMAX` (покриває 80 + 20 + частково 30)
- UA H1 — **не чіпати** (уже коректна друга форма)
- у вступний абзац додати точну фразу 1–2 рази
- RU H1 → `Накладки для настольного тенниса Butterfly` (30 замість 0)

⚠️ `/nakladki` має CMS-оверрайд — правити в `content_blocks`, перевіряти живим curl.

**Час:** 30 хв. **Money:** так, головна money-категорія.

---

### 1.5. Багаті хаби поза sitemap, порожні — в sitemap

**Доказ (curl, довжина видимого тексту `<main>` / кількість H2):**

| Хаб | знаків | H2 | у sitemap | внутр. лінків |
|---|---|---|---|---|
| `zyre` | 3 222 | 9 | ❌ | сайтвайд (хедер) |
| `rozena` | 2 391 | 9 | ❌ | 0 (UA), 1 (RU) |
| `feint` | 268 | 0 | ✅ | 0 |
| `tackiness` | 185 | 0 | ✅ | 0 |
| `sriver` | 157 | 0 | ✅ | 0 |
| `impartial` | 145 | 0 | ✅ | 0 |
| `roundell` | 136 | 0 | ✅ | 0 |
| `ilius` | 123 | 0 | ✅ | 0 |

Meta description у всіх шести порожніх — один шаблон: «Серія {X}: накладки для настільного тенісу. Офіційний товар, гарантія, доставка по Україні. N моделей · від Y ₴».

Попит (Ahrefs ua): `butterfly feint/sriver/roundell/tackiness/ilius/impartial` = **0** усі. `rozena` 20, `butterfly rozena` 10, `zyre 03` 20, `butterfly zyre 03` 20.

**Фікс — замінити критерій, а не латати випадки:** умова входу в sitemap/index = «є унікальний контент-блок **І** ≥1 товар» замість «≥2 товари». Тоді:
- 6 порожніх → `noindex, follow` + геть із sitemap (лонгтейлу не втрачаємо — його немає);
- `rozena` → в sitemap (контент є, попит є);
- `zyre` → окремо, див. розділ про канібалізацію.

**Час:** 2 год. **Money:** так, кластер `/nakladki`.

---

### 1.6. Хвіст: 183 із 184 gear-карток мають нуль прози

**Доказ:** медіана prose-слів (рядки ≥12 слів) = **0** для кожної категорії: `aksessuary` 56/56 нулів, `chehly` 49/49, `odyag` 47/47, `obuv` 20/20, `myachi` 8/8, `setki` 3/3. Виняток — `stoly` (233 слова). Money для контрасту: `nakladki` медіана 461, `osnovaniya` 492, нулів 0.

Повне тіло `/ua/butterfly/setki/sitka-butterfly-europa`: «Сітки / Сітка Butterfly Europa / Butterfly / 3 640 ₴ / В наявності / В кошик / Характеристики / Тип / Сітка / Схожі товари».

368 URL (184×2) = **53% sitemap**.

**Фікс — диференційовано, не глухий noindex:**
- `aksessuary` 56 + `chehly` 49 + `odyag` 47 + `setki` 3 = 155 URL/локаль: попит на рівні моделі нульовий (`шорты butterfly` 0, `кросівки butterfly` 0, `мячи butterfly` 0) → `noindex, follow`. **Follow критично** — вони й далі ллють вагу в money через крос-сел.
- `obuv` 20: єдина gear-група з карточним попитом (`butterfly lezoline` 10, `butterfly lezoline rifones` 10) → лишити index, прогнати генератор опису.
- `myachi` 8, `stoly` 1: лишити.

⚠️ Для `setki` (3 товари) і `myachi` noindex карток можна робити **тільки одночасно** з наповненням тексту самої категорії — інакше лістинг із 100% noindex-дітей стає soft-thin.

**Головний виграш не тут:** попит у хвості живе на **категоріях**, а не на картках — `сетка для настольного тенниса` 200 + `сітка для настільного тенісу` 200, `кросівки для настільного тенісу` 350 + `кроссовки` 150, `мячи для настольного тенниса` 70. Це 7 сторінок, а не 183. Увесь текстовий бюджет — туди.

**Час:** 1 год на noindex + 4–6 год на 7 категорійних текстів. **Money:** непрямо (частка якісного контенту домену).

---

### 1.7. `/ua/osnovaniya` = 735 КБ HTML через подвійний рендер сітки

**Доказ:** `curl -w '%{size_download}'` → 735 113 B (br 39 606 B). Розбір:
- `<!--$?--><template id="B:0">` @33610 → Suspense-fallback з повною сіткою 84 карток на `next/image` (84 `srcSet`, 178 452 B = 24%)
- `<div hidden id="S:0">` @358672 → **ще 84 картки**, легким компонентом
- `$RC("B:0","S:0")` — підміна
- Ділянка 33610→358672 = **325 062 B (44%)**, RSC-payload 248 152 B (34%)
- Товарних `<a href>` у HTML: 171 (84 унікальні × 2)

Те саме на `/ua/nakladki` (425 614 B, B:0@33686, S:0@205857). `/ua/rakety` (539 905 B) — інша причина: 285 img на 95 комбо-карток (основа + 2 накладки).

**Фікс:**
1. **−178 КБ, нульовий ризик:** у Suspense-fallback віддавати ту саму легку картку, що вже використовує `CatalogFilters` (плоский `<img>` на Cloudinary з `f_auto,q_auto,dpr_auto`), а не `next/image` — Cloudinary вже робить усе, що потрібно.
2. **−325 КБ:** не рендерити сітку двічі — сітка на сервері один раз, фільтри гідратуються поверх тієї самої розмітки.

⚠️ **НЕ** замінювати fallback на скелетон: тоді всі 84 `<a href>` на money-картки опиняться тільки всередині `<div hidden>` до виконання `$RC` — шлях хаб→картка стане JS-залежним.

**Час:** 3–4 год. **Money:** так, дві найважчі money-категорії.

---

### 1.8. Нуль аналітики й нуль верифікації GSC

**Доказ:** `grep -oiE '(googletagmanager|google-analytics|gtag\(|GTM-|google-site-verification|fbq\(|_vercel/insights|dataLayer)'` по `/ua`, `/ru`, `/ua/osnovaniya`, `/ua/nakladki`, `/ua/butterfly/nakladki/tenergy-05` → **0 збігів на всіх п'яти**. 12 `<script src>` — усі `/_next/static/chunks/*`. `/_vercel/insights/script.js` → 404. `/google*.html` → 404.

**Але:** трекінг-шар **уже написаний** — у `/_next/static/chunks/6664-b1e1317850136622.js` є `window.dataLayer.push`, `window.fbq("track",…)`, `window.gtag("event","conversion",{send_to: adsId/adsLabel})`, мапінг `view_item→ViewContent` і конфіг `{gtm, ga, adsId, adsLabel, pixel}` з порожніми дефолтами. ID просто не заповнені в CMS.

Побічно: `/ua/privacy` стверджує «Сайт використовує файли cookie … аналітики (Google Analytics / GTM, Meta Pixel)» — юридичний документ описує неіснуючу обробку. Cookie-банера немає.

**Наслідок:** GSC показує дані **лише з моменту верифікації**. Перші тижні після флипу — найцінніші дані (які запити реально дають покази на `/nakladki` і `/osnovaniya`) — не збережуться ретроспективно.

**Фікс:** заповнити GA4-ID (GTM зайвий шар, поки інших тегів немає) + `<SpeedInsights/>` (ендпоінт `/_vercel/speed-insights/script.js` уже віддає 200, бракує клієнта) + GSC URL-prefix property на vercel.app через `metadata.verification.google`, а domain property на `ttmax.com.ua` через DNS TXT одразу після реєстрації.

⚠️ Або Consent Mode v2 з дефолтом `denied`, або одночасно з cookie-банером — політика сама апелює до згоди.

**Час:** 2 год. **Money:** непрямо, але без цього стратегію «вся вага на накладки й основи» неможливо верифікувати.

---

### 1.9. Локальний шар відсутній, хоча сайт скрізь заявляє Харків

**Доказ:** title `/ua/about` = «…магазин настільного тенісу Butterfly **у Харкові**», `/ua/contacts` = «…**у Харкові**», `/ua/delivery` = «…**(Харків)**». У RSC-payload лежить `"address":{"street":"","locality":"Харків","region":"Харківська область","postal":"","country":"UA"}` — **і ніде не рендериться**. `Organization` має тільки `name, url, logo, sameAs, contactPoint` — без `address`, `openingHours`, `areaServed`.

Ahrefs ua: **`настольный теннис харьков` = 50/міс, KD 0, CPC $10** — найдорожчий і найлегший з усіх запитів у цьому аудиті, і він не таргетований ніде. Поруч: `магазин настільного тенісу` 40, `все для настільного тенісу` 70.

**Фікс:** після появи реального телефону — заповнити `address` + `openingHours` в `Organization` (не `LocalBusiness`, якщо немає точки для відвідувачів), додати місто в `/contacts` і `/delivery` природним текстом. Заодно виправити граматику: `/ua/contacts` дослівно каже «Ми у **Харків**» замість «у Харкові».

**Час:** 1 год. **Money:** так — 50/міс з CPC $10 і KD 0 на комерційному інтенті.

---

### 1.10. E-E-A-T: власний КМС є, але його немає на жодній money-картці

**Доказ:** `/ua/author/artem-maksymchuk` має `Person` JSON-LD: `jobTitle: "Гравець настільного тенісу (КМС), тренер"`, «Понад 140 зіграних турнірів», заповнений `knowsAbout`, 3 верифіковані зовнішні профілі. Але `grep -l 'Максимчук'` по **всіх 127 UA money-картках → 0 файлів**; `author`/`reviewedBy` в `Product` немає. Блок оцінок на картці підписаний анонімно — «Оцінка магазину» (Швидкість 9.0 / Контроль 8.0 / Жорсткість 7.8), і цих оцінок немає в JSON-LD взагалі.

**Фікс:** підписати блок оцінок іменем експерта з лінком на `/author/...`, додати `author` в `Product` JSON-LD. Дані вже написані — потрібно тільки з'єднати. **НЕ** додавати `aggregateRating` без реальних відгуків від користувачів — це structured-data spam.

**Час:** 1–2 год. **Money:** так, диференціація саме на 254 money-URL.

---

## P2 — гігієна

| # | Що | Доказ | Дія |
|---|---|---|---|
| 1 | Title `/odyag`: `«Ціна від , доставка»` в description+og+twitter, обидві локалі | `curl /ua/odyag`, `/ru/odyag`. Причина: всі 47 видимих товарів = «Ціна за запитом», мінімум по порожній множині. Токен `{{price_from}}` у CMS резолвиться в порожнечу | Рендерер має вирізати **клауз цілком**, а не лишати кому. Виправити в спільній утиліті — 13 блоків використовують `{{price_from}}` |
| 2 | `/odyag` інтро обіцяє «160 моделей від 208 ₴» при 47 видимих товарах, усі «Ціна за запитом» | Розбіжність intro↔meta лише в odyag (перевірено всі 10 категорій) | Привести intro і meta до одного scope (видимі товари) |
| 3 | «43 моделей» замість «43 моделі»/«43 модели» на `/nakladki`, 4 рядки (meta+intro × 2 локалі) | `curl` обох; `/osnovaniya` коректно «84 моделі». Перевірено всі 10 категорій — зламана одна | Виправити + прибрати ручне слово, взяти плюралізатор, що вже коректно обслуговує серійні хаби |
| 4 | 333 із 696 title > 60 знаків; max 90 (`Основа Butterfly Harimoto Tomokazu Innerforce Super ZLC — купити в Україні \| ціна, відгуки`) | Прогін усіх 696 | Прибрати хвіст `\| ціна, відгуки` (обіцяє відгуки, яких немає): min 43 / max 74, >60 лише 5/127. Для 5 найдовших основ додатково відкидати «в Україні» |
| 5 | 128 URL віддають `Product` без `offers` (5 із них money: `roundell`, `roundell-soft`, `bugller`, `feint-ox`, `fan-zhendong-cnf`) | Прогін 311 товарних URL | Це **не баг розмітки** — у товарів немає ціни, генератор чесно опускає `offers`. Проставити ціни або прибрати з sitemap. **НЕ** емітити `price: "0"`/`PreOrder` — порушення policy |
| 6 | У 122 наявних `Offer` немає `shippingDetails`, `hasMerchantReturnPolicy`, `seller`; `sku` = слаг, `gtin/mpn` відсутні | Прогін 254 money-URL, набір ключів ідентичний | Дані є: `/returns` → 14 днів, `/delivery` → поріг 5000 грн. Але спершу з'ясувати `returnFees` (хто платить зворотну пересилку) — без нього об'єкт неповний. `gtin` не вигадувати |
| 7 | 230 Product без `image` (chehly 98/98, setki 6/6, aksessuary 90/112, obuv 28/40, myachi 8/16) | Прогін 311 URL | Фото немає **взагалі** (ні `og:image`, ні `<img>`). Пріоритет заливки: obuv (150/міс) → setki (400/міс, 3 фото) → myachi → chehly → aksessuary. **НЕ** ховати через `hidden.ts` категорію з власним попитом |
| 8 | 1 094 `<img alt="">` на 127 money-картках (≈8.6 на картку); alt має тільки перше фото галереї | Краул money-карток; на лістингах порожніх alt 0 | Локальний дефект компонента галереї. ×2 локалі ≈ 2 188 фото невидимі для Google Images |
| 9 | 256 із 696 URL без `og:image`, у т.ч. головна (обидві локалі) і **`/rakety`** (2000+1000/міс) — при `twitter:card=summary_large_image` | `grep -ci og:image` (репортер помилився з регістром — потрібен `-i`) | Глобальний fallback у root metadata через ту саму Cloudinary-трансформацію `w_1200,h_630`. Money-ядро (`/nakladki`, `/osnovaniya`, всі їхні картки) — чисте |
| 10 | 5 однотоварних хабів віддають 200 поза sitemap (`zyre`, `rozena`, `bryce`, `aibiss`, `flextra`); `bryce/aibiss/flextra` — по 79–87 знаків, 0 вхідних лінків | curl всіх 10 URL | Винести поріг у **спільний хелпер** для routing + sitemap + навігації. Для 1-товарних: 301 на картку або `index:false + follow` |
| 11 | 6 серійних хабів у sitemap мають 0 внутрішніх лінків; водночас `zyre` (не в sitemap) лінкується сайтвайдно з хедера | Краул усіх 348 UA-URL | Дзеркальна неузгодженість меню vs sitemap. Прибрати `zyre` з хедера або довести хаби до порогу |
| 12 | `lastmod` = час рендеру: 686 із 696 URL мають штамп, що збігається з `date` відповіді до секунди | `curl /sitemap.xml`; реальні дати мають лише 10 URL = 5 статей блогу × 2 | Не віддавати `lastmod`, якщо реальної дати немає. Прибрати `changefreq` і `priority` (Google ігнорує обидва) |
| 13 | `SearchAction.target` → `/{locale}/search?q=`, який віддає 404, на всіх 696 URL | `curl '/ua/search?q=tenergy'` → 404 | Прибрати `potentialAction` з `WebSite` JSON-LD (Sitelinks searchbox виведений з Google у 2023). Пошук будувати як UX-фічу окремо, з `noindex` |
| 14 | Немає `apple-touch-icon`, `theme-color`, manifest; `/favicon.ico` 404 | Усі 404: `/favicon.ico`, `/site.webmanifest`, `/apple-touch-icon.png`, `/icon.png` | Додати `apple-icon.png` 180×180 + `theme-color`. ⚠️ `app/icon.png` **переб'є** CMS-керований фавікон — icon лишити за CMS |
| 15 | 404-сторінка віддає canonical, title, og:url і 3 hreflang **головної** | `curl /ru/nonexistent` → 404 + `canonical=/ru` | Правити в `generateMetadata` catch-all (не в `not-found.tsx` — він не перекриє метадані батька). Прибрати canonical/hreflang/og |
| 16 | 22 RU-сторінки з українськими назвами в title, H1, breadcrumb і `Product.name` | `/ru/butterfly/odyag/shtany-butterfly-vf` → «Штани Butterfly VF»; 11×Штани, 4×Пляшка, 3×Очищувач, 2×Шорти, 1×Очисник, 1×Бавовняна. Плюс 91 `alt` | Locale-keyed RU-оверрайд (**не** глобальний rename — зламає UA). Слаги не чіпати |
| 17 | `/ru` головна: «Тільки орігінал» (і українською, і з орфографічною помилкою) | Скан усіх 348 RU-сторінок на ї/і/є/ґ → рівно 1 забруднена. Джерело: `overrides.statValue3` в CMS, ідентичний для обох локалей | Найпростіше — **очистити** `statValue3`/`statLabel3` в CMS: спрацює i18n-дефолт, який уже коректно локалізований |
| 18 | H1 `/odyag` = «Одяг **та взуття** для настільного тенісу» містить H1 `/obuv` цілком | curl обох, обидві локалі. `взуття для настільного тенісу` = 40/міс | UA H1 → «Одяг для настільного тенісу», RU → «Одежда для настольного тенниса». Згадки взуття в тілі **лишити** — це коректна перелінковка |
| 19 | 5 кольорових варіантів Lezoline як окремі URL | `rifones` + `-syni` + `-chorni`: та сама ціна 6 234,8 ₴, ті самі розміри, у клонів **0 власних фото** (у базової 6) | Rifones: прибрати 2 клони. Groovy **не чіпати** — це різні SKU (4 674,8 / 3 640 / 3 640 ₴, один лише 44 р.), canonical зламає ціну в SERP |
| 20 | Фасети `antyspin`/`dovgi-shypy`/`korotki-shypy` як сирі слаги в UI фільтрів `/nakladki` | 3 видимі кнопки на UA + 3 на RU. Словник **уже є** — ті самі значення підписані в `surfaceType` | Дописати 3 ключі в мапу `playStyle`. **Тільки label**, не value — на value зав'язані URL |
| 21 | `/stoly` — 0 контентних H2 і 0 вихідних крос-лінків (єдина з 10 категорій) | У `<main>` рівно 2 лінки: breadcrumb + 1 товар. «Дивіться також» є на всіх 9 інших | Додати крос-сел → `/setki`, `/myachi`, `/rakety` + зворотний лінк на `/blog/rozmiry-tenisnogo-stolu` (зараз стаття лінкує двічі, категорія — нікуди) |
| 22 | Дублі FAQ: `«ALC чи ZLC?»` байт-у-байт на `/osnovaniya` і `/osnovaniya/alc` | Розбір JSON-LD; єдиний точний дубль на 82 пари Q/A | Перефразувати питання. **НЕ** видаляти блок «ALC проти ZLC» з `/alc` — там крос-лінк «Дивитись ZLC →» |
| 23 | Блог: `/blog` немає у футері; головна не має жодного контекстного лінка на статті | Дамп футера: є `/about`, `/aksessuary`, `/myachi`, `/odyag`, `/stoly`, немає `/blog`, `/rakety`, `/obuv`, `/chehly`, `/setki` | Додати в футер. На головну — блок «Читайте» з `/blog/ping-pong` першим (пінг понг 600 RU + 200 UA). **НЕ** ставити «Корисне» на `/nakladki` і `/osnovaniya` |
| 24 | Квізи: результати рендеряться тільки після кліку, лінків немає в SSR-HTML | Бандл `page-5e4baffb950e437a.js`: `u&&(...)`, де `u=null` до кліку | Тільки для `/blog/pravyla-nastilnogo-tenisu` (там 0 money-лінків): статичний рядок `<a>` під квізом. Для `yak-vybraty-raketku` — no-op, там усі 6 таргетів уже статичні |
| 25 | `no-store` + `x-vercel-cache: MISS` на всьому HTML; функція в `iad1` при UA-аудиторії | `x-vercel-id: arn1::iad1::…`, TTFB 0.35–0.47 s | **Спершу регіон** → `fra1`/`arn1` (0 ризику). ISR — тільки після вебхука Supabase → `revalidateTag`, інакше правки CMS зависатимуть до години |
| 26 | 195 КБ шрифтів у `preload` на кожній сторінці (16 файлів `.woff2`) | Заголовок `link:` | Скоротити до реально використовуваних накреслень; `no-store` змушує передомовляти щоразу |
| 27 | Обкладинки блогу: 777 КБ PNG у герої, `preload as=image`, без `next/image` | `/blog/rozmiry-cover.png` 777 281 B; сумарно 5 файлів ≈ 2,1 МБ. `/blog` вантажить усі 5 одразу без `lazy` | Через оптимізатор: 777 КБ → 13,7 КБ AVIF (**−98%**). `loading="lazy"` для 4 із 5 на хабі |
| 28 | Cloudinary віддає `cache-control: private` — спільні кеші не зберігають | `curl -I` origin | Налаштувати `public` на origin |
| 29 | Немає `dateModified` на 622 картках; немає внутрішнього пошуку; `/cart`, `/checkout`, `/thank-you` = 404 | `grep dateModified` → 0; `<form>`/`<input>` на картці = 0 | Без URL подяки нічого буде відстежувати в GA4 як конверсію |
| 30 | БД: `CHECK (locale IN ('uk','ru'))` vs Zod `'ua'|'ru'`; у `content_blocks` дублі `ua`+`uk` на 12 слагів, CHECK відсутній | `pg_constraint` + SELECT | Канон = `uk`/`ru` (ISO). Одна функція `toDbLocale()`. Звести дублі в `content_blocks` і додати CHECK — **саме там** дефект уже живий і зачіпає `/nakladki` |

---

## Окремо: КАНІБАЛІЗАЦІЯ

### Знайдено (реальна — спільний запит з підтвердженим обсягом)

| Пара | Спільний запит | Обсяг ua |
|---|---|---|
| `/nakladki/zyre` vs `/butterfly/nakladki/zyre-03` | `zyre 03` / `butterfly zyre 03` | 20 + 20 |
| `/nakladki/glayzer` (title містить «Glayzer 09C») vs картки `glayzer` **і** `glayzer-09c` | `butterfly glayzer`, `glayzer 09c` | 20 + 20 |
| `/nakladki/rozena` vs `/butterfly/nakladki/rozena` | `rozena` / `butterfly rozena` | 20 + 10 |
| `/nakladki/sriver`, `/nakladki/roundell` vs однойменні картки | — | **0** (структурний дефект без грошей) |
| `/odyag` H1 містить H1 `/obuv` цілком | `взуття для настільного тенісу` | 40 |

**Спільний корінь усіх п'яти:** slug серії дорівнює slug товару, або title хаба містить номер моделі. Механічна перевірка: перетин множин `{хаби серій} × {слаги товарів}` по sitemap дає рівно 3 (`glayzer`, `sriver`, `roundell`), плюс 2 однотоварні (`zyre`, `rozena`).

**Системний фікс (одне правило замість п'яти правок):** якщо slug серії збігається зі slug товару в тій самій категорії **або** серія має <2 товарів → `index:false, follow` (той самий патерн, що вже прийнятий для 95 комбо-ракеток за правилом №4). Лінки в навігації **лишити** — follow передає вагу на обидві картки.

⚠️ **Не** робити «чисту серійну форму» для Glayzer: `Накладки Butterfly Glayzer` — це ідеальний дубль title картки `/butterfly/nakladki/glayzer`. Саме ця помилка вже живе на `/nakladki/roundell` і `/nakladki/sriver`.

⚠️ Title money-карток **не чіпати** — вони вже коректно таргетять моделі.

### Перевірено і НЕ є канібалізацією

| Гіпотеза | Чому спростовано |
|---|---|
| `/blog/yak-vybraty-raketku` vs `/rakety` | Різні запити: `ракетки для настільного тенісу` 1000 (транзакційний) vs `як вибрати ракетку…` 50 (інформаційний). Внутрішня вага асиметрична на порядки: `/rakety` має 2–3 лінки з кожної з ~350 сторінок, стаття — 7. Стаття вже лінкує 6 money-карток. Запропонований фікс вирізав би єдиний живий таргет статті |
| Дублі FAQ між `/nakladki` і блогом | `alc чи zlc`, `tenergy vs dignics`, `які накладки обрати новачкові` — Ahrefs ua повертає **нуль** по всіх формах. Спільного запиту не існує. Плюс FAQ rich results з 08.2023 доступні лише gov/health-сайтам — заявлений механізм шкоди мертвий |
| Хвостові серійні хаби vs `/nakladki` | `butterfly feint/sriver/roundell/tackiness/ilius/impartial` = 0. Голе `feint` 80 — це музикант Monstercat, `impartial` 80 — англійське слово. Це thin-контент, не канібалізація |
| `/butterfly` бренд-хаб vs головна | Ahrefs ua повертає **порожній масив** по `butterfly настольный теннис`, `баттерфляй настольный теннис`, `butterfly ракетки`, `butterfly накладки`. Реальний дефект інший — див. перелінковку |
| `/nakladki/alc`-групи vs `/osnovaniya` | `основание alc`, `alc или zlc`, `основа alc` — Ahrefs не повертає рядків узагалі |
| «вискария» 40 / «віскарія» 80 як таргет для Viscaria | SERP ua на 90–100% садівничий: Silene viscaria, ліхніс, «посадка и уход», насіння. Дезамбігована форма `вискария основание` = 10. **Не додавати** «Віскарія» в UA — це 80/міс чужого інтенту на money-картку |

---

## Окремо: ПЕРЕЛІНКОВКА — карта потоку ваги

### Зараз (виміряно на повному графі 441 сторінка, nav+footer+main, 38 397 лінків)

```
                    ┌─ header (×2 рендери: desktop + mobile drawer) ─┐
                    │  19 money-лінків / 10 tail — на КОЖНІЙ сторінці │
                    └────────────────────┬───────────────────────────┘
                                         ▼
  /ua ──21 контекст.──► /nakladki (PR 0.0939 #1) ──43 картки──► money-картки
   │                    /osnovaniya (PR 0.0794 #2) ─84 картки──►  (avg PR 0.0017 / 0.0012)
   │                         │
   │                         └──► /osnovaniya/alc (17), /zlc (16) ✓ працює
   │
   ├──► /butterfly ──10 лінків──► /butterfly/{category} (noindex) ──44──► картки ✓
   │         ▲ сайтвайдний анкор «Колекції»       └─ але /nakladki і /osnovaniya
   │           з усіх ~700 URL                       з бренд-хаба НЕ отримують НІЧОГО
   │
   ├──► /rakety ─95──► комбо (noindex,follow) ─3 з 8 лінків──► money-картки ✓
   │
   └──► /blog (нав) ─5──► статті ──19 money-лінків з рейтингової ✓
             ▲ немає у футері      └─ 3 з 5 статей: 0 лінків у /nakladki+/osnovaniya

  ХВІСТ (368 URL): TAIL→TAIL 922 з 1120 вхідних (82%), MONEY→TAIL = 0
```

**PageRank підсумок:** MONEY 49.3% / TAIL 35.4% / RAKETY 6.6%. `/nakladki` і `/osnovaniya` — сторінки №1 і №2 сайту, вище за головну (0.0336). Середній PR money-картки вищий за **кожну** категорію хвоста.

### Що реально зламано (а не «здається зламаним»)

1. **88 контекстних лінків через 308** на money-гілці — див. P1.2.
2. **14 money-SKU з 0 вхідних cross-sell** — див. P1.3.
3. **Бренд-хаб `/butterfly` не годує money.** Його 10 контентних лінків ведуть виключно на `/butterfly/{category}` (noindex, викинуті з sitemap). Перетин з набором лінків `<main>` головної = **0**. При тому, що `/butterfly` — це сайтвайдний анкор «Колекції» з усіх ~700 URL. **Фікс на 1 файл:** перепризначити сітку категорій з `/{locale}/butterfly/{category}` на канонічні `/{locale}/{category}` + додати блок серій (`tenergy`, `dignics`, `glayzer`) і `/osnovaniya/{alc,zlc}`.
4. **`/blog` немає у футері**, як і `/rakety`, `/obuv`, `/chehly`, `/setki` — при тому що `/odyag`, `/aksessuary`, `/myachi`, `/stoly` там є. `/rakety` під найбільший запит сайту (2000+1000) має **1 унікальне розміщення** проти 3 у хвостових `/odyag` і `/myachi`.
5. **Анкор `/rakety`:** 232 з 235 контекстних анкорів побудовані на фразі «готова ракетка», яка в Ahrefs ua **не існує взагалі** (matching-terms по «готова ракетка, готовая ракетка» → порожній масив). Головну форму `ракетки для настільного тенісу` (1000) використовує рівно **1** лінк на весь сайт.

### Що НЕ треба чіпати (перевірено)

- **Фасетні лінки в хедері.** 7 із 10 фасетів self-canonical на `/nakladki` і `/osnovaniya` — вони **додають** money-категоріям консолідовані частки, а не розмивають. `rel="nofollow"` на них = чиста втрата. Мобільний drawer теж лишити — дубль `<a>` на той самий URL не подвоює передачу ваги.
- **CTA «Готова ракетка з цією основою →» на 127 картках.** `/rakety` має 139 контекстних inbound, з них 127 — це саме ці CTA. Прибрати = зрізати 91% контекстної ваги з категорії на 3000/міс.
- **Хлібні крихти карток.** Вставляти `/nakladki/{series}` проміжною ланкою = подовжити шлях категорія→картка і відвести PageRank на 9 сторінок з нульовим попитом.
- **Блок «Серії Butterfly» з 11 лінками на `/nakladki`.** Додасть 6 вихідних лінків на хаби з нульовим попитом і розбавить вагу, що зараз іде на 43 картки.

### Цільова схема (money-first)

```
Головна ──► /nakladki, /osnovaniya (пряме, вже є) + блок «Читайте» (2-3 статті)
/butterfly ──► /{locale}/{category} (замість noindex-шару) + серії + топ-картки
/nakladki ──► 43 картки (вже) ; /osnovaniya ──► 84 картки + alc/zlc (вже)
картка ──► категорія + 4 РЕЛЕВАНТНІ сусіди (скоринг) + /rakety CTA
хвіст ──► власна категорія + nav (як зараз) ; noindex,follow для 155 URL
футер ──► + /blog, /rakety, /obuv, /chehly, /setki
```

---

## Окремо: УНІКАЛЬНІСТЬ ОПИСІВ

### Категорії — **чисто, з великим запасом**

Попарна схожість SEO-прози (5-грамний Jaccard, товарні картки вирізано): максимум **0.103** (`glayzer↔rozena`), далі 0.096 (`dignics↔tenergy`); категорії між собою до 0.083. Порядковий diff `glayzer` vs `rozena` показав **нуль** спільних речень окрім двох сайтвайдних рядків — перетин тримається виключно на технічній лексиці (Spring Sponge, Shore A/ESN).

Обсяг прози (всі >500 знаків, тобто правило проєкту витримано):

| Сторінка | знаків | H2 |
|---|---|---|
| `/rakety` | 6 716 | 4 |
| `/nakladki` | 2 428 | 8 |
| `/osnovaniya` | 2 369 | 4 |
| `/osnovaniya/alc` | 1 991 | — |
| `/osnovaniya/zlc` | 1 800 | — |
| `/stoly` | 1 559 | **0** ⚠️ |
| `/setki` | 1 571 | 6 |

Перетин `/osnovaniya/alc` з батьківською `/osnovaniya` = **6.9%** символів, `/zlc` = 4.7%. Це повноцінні сторінки, не фасети.

**RU не є копією UA** — перевірено в **обидва** боки: регекс на ї/і/є/ґ по всіх 10 RU-категоріях → 0; регекс на ы/э/ъ/ё по всіх 10 UA → 0. Опис Viscaria: UA 1 260 знаків / RU 1 265, різні формулювання («відскік»/«отскок», «дзвінким»/«звонким»).

**Єдина реальна діра:** `/stoly` — 0 контентних H2 (всі 9 інших категорій мають 6–8) і 0 вихідних крос-лінків.

### Money-картки — **чисто по тексту, зламано по метаданих**

- 43 UA-картки `/nakladki`: max Jaccard **0.088** (`tenergy-64-fx ↔ tenergy-80-fx`), мінімум prose 1 854 знаки, медіана ~3 700
- 84 картки `/osnovaniya`: max Jaccard **0.065** (`timo-boll-alc ↔ viscaria`), решта <0.035, мінімум 1 986 знаків
- 0 дублів `<title>`, 0 дублів `<h1>`, 0 карток з описом <400 знаків, 0 без `Product.image`

**Але:** 1 шаблон meta description на всі 254 URL; 1 шаблон title з фейковою обіцянкою «відгуки»; жодної кириличної транслітерації; жодного `author`. Тобто **текст унікальний на 100%, а вітрина в SERP — на 0%.**

Єдиний точний дубль Q&A на 617 питань/148 сторінок: «Яку ручку обрати?» на 3 картках основ і «Яку основу обрати?» на 2+2 картках накладок (4 кластери × 2 локалі = 16 URL). При цьому 75 інших карток мають унікальні, предметні відповіді на те саме питання (falcima прямо каже, що ST/AN для неї не випускаються).

### Хвіст — **порожньо**

183 із 184 карток: **0 слів прози**. Набір H2 дослівно `['Характеристики','Схожі товари','Кошик']` у 12 з 12 випадково перевірених. Категорії хвоста: `aksessuary` 912 знаків, `setki` 962, `chehly` 985, `odyag` 1 160, `myachi` 1 257, `obuv` 1 297 — усі в межах правила, але це і є найтонші сторінки сайту, що правильно з точки зору money-пріоритету.

**Три з чотирьох гіпотез власника не підтвердилися:** шаблонних описів немає, RU не копія UA, порожніх категорій немає. Проблема сконцентрована рівно у двох місцях: **метадані money-карток** і **картки хвоста**.

---

## План дій по порядку

### Фаза A — блокери запуску (робити першими, послідовно)

| # | Дія | Час | Блокує запуск |
|---|---|---|---|
| 1 | Зареєструвати `ttmax.com.ua`, підключити до Vercel, зробити **Primary** (без www; www → 308 на apex). Перевірити 200 по HTTPS. **Не** чіпати robots/noindex | 1–2 год + DNS | **ТАК** |
| 2 | Відкрити `src/config/site.ts`, встановити (env чи константа), оновити хост, **чистий** redeploy. Перевірка: `curl /sitemap.xml \| grep -c vercel.app` → 0 | 1 год | **ТАК** |
| 3 | Реальний телефон у `contact.phone` (E.164) + `contact.phoneDisplay`; перемкнути JSON-LD на `phone`. Або прибрати блок цілком. Заодно 3 соцкнопки `href="#"` | 1 год | **ТАК** |
| 4 | `/contacts` + `/terms` + `/privacy`: прибрати «додамо найближчим часом», однозначне позиціонування, блок реквізитів (ФОП/ЄДРПОУ), працюючий e-mail. Виправити «Ми у Харків» | 2 год | **ТАК** |
| 5 | Прибрати з `/privacy` згадку Meta Pixel/GTM або поставити їх; cookie-consent | 1–2 год | **ТАК** (юр.) |
| 6 | GA4 (ID у CMS — код уже написаний), `<SpeedInsights/>`, GSC domain property на `ttmax.com.ua` через DNS TXT | 2 год | **ТАК** |
| 7 | **Preview-деплой з `launched=true`** (закритий доступ, не публічний прев'ю!). Прогнати: meta robots на 10 типах роутів, sitemap vs robots, canonical/hreflang/og на 4 URL (категорія + картка × 2 локалі) | 2 год | **ТАК** |
| 8 | Флип `launched=true`: спершу зняти `<meta noindex>` → deploy → переконатися → **потім** robots.txt. Не навпаки — інакше вікно, коли Googlebot краулить сторінки з noindex | 1 год | **ТАК** |
| 9 | Подати sitemap у GSC, запросити індексацію `/ua`, `/ua/nakladki`, `/ua/osnovaniya` | 15 хв | — |

### Фаза B — money (робити паралельно з A, до флипу)

| # | Дія | Час | Блокує |
|---|---|---|---|
| 10 | **R1:** передати повний record товару в `generateMetadata` і `Product` JSON-LD. Description із вердикту (3 різні поля, умовна гілка по ціні для 5 карток без `price`) | 3 год | Ні, але дешевше до індексу |
| 11 | `UPDATE content_blocks` `/uk/`→`/ua/` (88 лінків) + CI-guard | 1 год | Ні |
| 12 | Title money-карток: прибрати `\| ціна, відгуки`; для 5 найдовших основ — умовне відкидання «в Україні» | 1 год | Ні |
| 13 | `/nakladki` title/H1/інтро: `накладки на ракотку` (80/міс) — через CMS, перевірити curl. RU H1 → форма з ненульовим обсягом | 30 хв | Ні |
| 14 | Cross-sell: скоринг + мінімальний in-degree ≥3 + ручний список власника для топ-20 | 4–6 год | Ні |
| 15 | Хаби серій: спільний хелпер порогу (routing + sitemap + навігація), критерій = «є унікальний текст», `rozena` в sitemap, 6 порожніх — геть, `zyre`/`glayzer`/`sriver`/`roundell` — `index:false, follow` | 3 год | Ні |
| 16 | `/butterfly`: сітка категорій → канонічні `/{locale}/{category}` + блок серій | 1 год | Ні |
| 17 | Підписати блок оцінок іменем експерта + `author` в `Product` JSON-LD | 2 год | Ні |
| 18 | Кириличні форми **тільки в RU-тіло**: `дигникс 05`, `тенерджи 05`, `тенерджи 19`, `дигникс 09с`; Viscaria — лише зв'язка «вискария основание». UA-форми не додавати (0/міс) | 1 год | Ні |
| 19 | `Offer`: `hasMerchantReturnPolicy` (14 днів) + `shippingDetails` (поріг 5000 грн) — після з'ясування `returnFees` | 2 год | Ні |
| 20 | `настольный теннис харьков` (50, KD 0, CPC $10): `address`+`openingHours` в `Organization`, місто в `/contacts` і `/delivery` | 1 год | Ні |

### Фаза C — продуктивність і хвіст (після флипу, крім C1)

| # | Дія | Час | Блокує |
|---|---|---|---|
| 21 | **Регіон функції** `iad1` → `fra1`/`arn1` | 15 хв | Ні (робити зараз — 0 ризику) |
| 22 | Прибрати подвійний рендер сітки (`/osnovaniya` −44%, `/nakladki` аналогічно) + легка картка у fallback (−178 КБ) | 3–4 год | Ні |
| 23 | Обкладинки блогу через оптимізатор (777 КБ → 13,7 КБ) + `lazy` для 4 із 5 на `/blog` | 1 год | Ні |
| 24 | Хвіст: `noindex, follow` для 155 URL (aksessuary/chehly/odyag/setki), `obuv`/`myachi`/`stoly` лишити index | 1 год | Ні |
| 25 | Текст на 7 gear-категорій (пріоритет: `setki` 400/міс, `obuv` 500/міс, `myachi` 170/міс) | 6 год | Ні |
| 26 | Фото: `setki` 3 шт → `obuv` 14 → `myachi` 4 → `chehly` 49 → `aksessuary` 45 | за наявності | Ні |
| 27 | `/stoly`: крос-сел + 2–3 H2 + зворотний лінк на статтю про розміри | 1 год | Ні |
| 28 | Футер: + `/blog`, `/rakety`, `/obuv`, `/chehly`, `/setki` | 15 хв | Ні |
| 29 | ISR — **тільки після** вебхука Supabase → `revalidateTag`; спершу перевірити `cookies()`/`headers()` у layout (інакше `revalidate` мовчки ігнорується) | 4 год | Ні |
| 30 | Локалі: `toDbLocale()` в одному місці, звести дублі `ua`/`uk` в `content_blocks`, додати CHECK | 3 год | Ні |
| 31 | Решта P2 з таблиці (`lastmod`, `SearchAction`, 404-метадані, `alt`, RU-назви, фасети, favicon) | 6 год | Ні |

**Разом до флипу:** ~10–14 год роботи + очікування DNS. **Фази B і C:** ~50 год, розподіляються на 2–3 тижні після запуску.